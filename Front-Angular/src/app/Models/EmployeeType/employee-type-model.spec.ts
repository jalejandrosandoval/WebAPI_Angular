import { EmployeeTypeModel } from './employee-type-model';

describe('EmployeeTypeModel', () => {
  it('should create an instance', () => {
    expect(new EmployeeTypeModel()).toBeTruthy();
  });
});
